import React from 'react';
import './AppBar';

function AppBar() {
    return (
        <nav className="AppBar">
            <div>Contacts</div>
            <div>Username</div>
        </nav>
    )
}   

export default AppBar;